#include <stdio.h>
#include "stack.h"

void main()
	{

		int valore = 5;

		while (valore != 0 )
			{
				push( valore );
				valore--;
			}

		while (valore <= 5 )
				{
					pop(valore);
					printf("valore: %d\t", valore);
					valore--;
				}





	}
