#include <stdio.h>
#include "stack.h"

void main()
	{

		int valore = 5;

		while (valore != 0 )
			{
				push( valore );
				valore--;
			}

		while (valore <= 54 )
				{
					pop(valore);
					printf("valore: %d\t", valore);
					valore--;
				}





	}
